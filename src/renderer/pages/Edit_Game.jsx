import { useParams, Link } from "react-router-dom";
import { useEffect, useState } from "react";
import styles from "../style/Edit_Game.module.css"

export default function Edit_Game() {
    const { id } = useParams();
    return (
        <>
        </>
    );
}
